import LoginForm from "components/LoginForm/LoginForm";

const Login = () => {

  return (
    <main style={{ textAlign: 'center' }}>
      <p>login</p>
      <LoginForm />
    </main>
  );

};

export default Login;
