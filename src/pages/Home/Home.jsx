import { Typography, Box } from '@mui/material';

const Home = () => {
  return (
    <main style={{ textAlign: 'center' }}>
      <Box sx={{ paddingTop: '24px' }}>
        <Typography variant="h4" gutterBottom>
          REGISTER AND CREATE YOUR CONTACT BOOK
        </Typography>
      </Box>
    </main>
  );
};

export default Home;
